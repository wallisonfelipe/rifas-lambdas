{
  "name": "rifas-payments",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:prod": "set -a && . ./.env.production && set +a && serverless deploy --stage production",
    "build:dev": "set -a && . ./.env.dev && set +a && serverless deploy --stage dev",
    "dev": "serverless offline start",
    "database:start": "DYNAMO_ENDPOINT=http://localhost:8010 dynamodb-admin --port=8010",
    "seed": "node scripts/seed.js",
    "test:generate-numbers": "node scripts/local-test-generate-numbers.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.585.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.585.0",
    "axios": "^1.7.0",
    "express": "^4.19.2",
    "mersenne-twister": "^1.1.0",
    "pg": "^8.13.0",
    "pg-copy-streams": "^6.0.6",
    "qrcode": "^1.5.4",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "serverless": "^4.32.2",
    "serverless-offline": "^14.3.4"
  }
}